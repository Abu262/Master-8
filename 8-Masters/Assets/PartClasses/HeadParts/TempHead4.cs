using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TempHead4 : HeadClass
{
    // Start is called before the first frame update
    void Start()
    {
        partName = "TempHead1";
        ID = 1;
        weight = 1000;
        hp = 500;
        radar = 4;
    }


    // Update is called once per frame
    void Update()
    {
        
    }

}